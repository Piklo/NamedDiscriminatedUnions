namespace NamedDiscriminatedUnions.Example;

internal class Program
{
    static void Main()
    {
        //    var union = new MyTestUnion(123);
        //    if (union.IsInt(out var item))
        //    {
        //        Console.WriteLine(item);
        //    }

        //    union.Switch(
        //        processInt: (i) => { Console.WriteLine($"im an int {i}"); },
        //        processLong: (l) => { Console.WriteLine($"im a long {l}"); },
        //        processDouble: (d) => { Console.WriteLine($"im a double {d}"); },
        //        processString: (s) => { Console.WriteLine($"im a string {s}"); },
        //        processMyTestClass: (i) => { Console.WriteLine($"im a class {i}"); },
        //        processMyTestStruct: (i) => { Console.WriteLine($"im a struct {i}"); });
    }
}
